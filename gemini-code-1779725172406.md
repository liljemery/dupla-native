# Dupla: Refactor de Arquitectura y Optimización de Pipeline (ETL Separado)

## Objetivo Principal
Reestructurar el pipeline de procesamiento de planos (DWG/PDF) para separar la extracción pesada (APS/Visión) del motor iterativo de cálculo y presupuesto. El objetivo es reducir el tiempo de las corridas de prueba de horas a segundos, mejorando radicalmente la Developer Experience (DX).

---

## Fase 1: Detener la hemorragia multiplicadora (Python)
Actualmente el orquestador ejecuta las 4 disciplinas por defecto, multiplicando el cómputo x4. Necesitamos forzar una disciplina única explícita o modo clasificación.

**Archivo a modificar:** `processor/tasks.py` (Orquestador principal, aprox. línea 228)
```python
def process_blueprint(file_id: str, disciplines: list[str] = None):
    # Forzar disciplina única o de clasificación en desarrollo
    if not disciplines or len(disciplines) == 0:
        print("⚠️ No se especificó disciplina. Ejecutando solo extracción base (Clasificación).")
        disciplines = ["base_extraction"] 
        
    # Limitar concurrencia masiva en modo DEV (frenar el x4)
    if len(disciplines) > 1 and os.getenv("ENVIRONMENT") == "development":
        print(f"⚠️ Limitando a 1 disciplina en DEV. Ignorando: {disciplines[1:]}")
        disciplines = [disciplines[0]]

    for discipline in disciplines:
        # Mantener lógica original de encolado
        enqueue_discipline_job(file_id, discipline)